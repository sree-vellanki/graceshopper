import React, { useState, useEffect } from "react";
import {Button, Modal, Tabs, Tab, Form} from "react-bootstrap";

const CartButton = () => {
    return (
        <div>
            <Button>cart</Button>
        </div>
    )
}